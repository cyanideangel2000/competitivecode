#include <iostream>
#include <bitset>

using namespace std;
char s[1000000] = {NULL, };
string str;

int main(void)
{
    int n, m, p , x;
    int i, j, k, l;
    int testcase;
    
    cin >> testcase;
    while (testcase)
    {
        cin >> n >> m >> p >> x;
        scanf("%s", &s);
        testcase--;
    }
}
