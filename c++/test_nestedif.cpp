#include <iostream>
using namespace std;

int main(void)
{
    int a, b, c;
    cin >> a >> b >> c;
    if (a)
        if (b)
            if (c) 
                cout << "3";
        else cout << "not 3 \n";
    return 0;
}
